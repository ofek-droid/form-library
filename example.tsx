//import {useFormValue, Form, FormControl}  from 'form-library'
/*
import React, { InputHTMLAttributes, ReactElement, ReactHTMLElement } from "react";

const notEmpty = (v: string) => v.length > 0? null : 'Field is empty';

interface InputAttribs {
    onChange: (e: any) => void
    onClick: (e: any) => void
}

interface IMyInput extends ReactElement<InputAttribs>{}

interface IProps {
    
    component: IMyInput
    errorComponent: IMyInput
}

const FormControl:React.FC<IProps> = ({component}) => {
    console.log(component)
     
    
    return <div></div>
}

export default () =>  {
 //   const formValue = useFormValue('',[notEmpty]);

    function onSubmit() {
        //do stuff- Api call, etc
    }

    return (
    <div>
        <FormControl type={'text'} component={<input />} 
            errorComponent={<input style={{border: '1px solid red'}}/>}/>
    </div>
  //      <Form onSubmit={onSubmit}>
            //<FormControl component={<input />} config={config} formValue={formValue} type="text" />
            
   //     </Form>
    )
}

*/
