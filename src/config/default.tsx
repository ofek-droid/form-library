import { FormConfig } from "../types/form-types";

const text = <input />

export const defaultConfig: FormConfig = {
    submitBehaviur: 'on_button_click',
    inputComponents: {
        text
    }
}
